# customer-ltv-prediction
Predicts 12-month customer lifetime value from purchase behavior using Random Forest &amp; XGBoost (R²=0.87). Segments customers into Low/Medium/High/VIP tiers — reveals top 10% of customers drive 50% of predicted revenue. Built with time-based train/test split to avoid data leakage.
